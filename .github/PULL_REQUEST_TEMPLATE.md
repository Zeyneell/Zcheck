## What this changes

## Checklist
- [ ] `ruff check src tests` passes
- [ ] `pytest -q` passes
- [ ] If adding/changing a checker: `zcheck doctor --only <site>` is not `degraded`
- [ ] No secrets, personal data, or local paths committed
